import { ChangeWeightUnitPipe } from './change-weight-unit.pipe';

describe('ChangeWeightUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeWeightUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
